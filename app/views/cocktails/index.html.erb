<div class = "container">
  <div class="row">
    <div class="text-center">
      <h1>Cocktails</h1>
        <% @cocktails.each do |cocktail| %>
          <div class = "col-md-6"><h3><%= link_to(cocktail.name, cocktail_path(cocktail)) %></h3></div>
           <br/>
        <% end %>
    </div>
  </div>
</div>