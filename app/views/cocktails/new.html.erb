<div class="container">
    <div class="row">
      <div class="col-sm-8 col-sm-offset-4">
        <h2>Add Cocktail</h2>
        <%= simple_form_for [@cocktail] do |f| %>
          <%=f.input :name %>
          <%= f.input :photo %>
          <%= f.input :photo_cache, as: :hidden %>
          <div class="submit"><%= f.submit "Add cocktail", class: "btn btn-primary" %></div>
          <% end %></li>
      </div>
    </div>
</div>
